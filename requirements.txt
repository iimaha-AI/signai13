# ============================================================
# SignAI — Python Requirements (Cloud / PostgreSQL edition)
# Python 3.10 or 3.11 recommended
# ============================================================

# ── Web Framework ──────────────────────────────────────────
Flask==3.0.0
Werkzeug==3.0.1
Flask-Cors==4.0.0
Flask-Limiter==3.5.0
python-dotenv==1.0.0
gunicorn==21.2.0

# ── Database (PostgreSQL) ──────────────────────────────────
psycopg2-binary==2.9.9

# ── AI / Machine Learning ──────────────────────────────────
tensorflow-cpu==2.15.0
numpy==1.26.4
scikit-learn==1.4.0
h5py==3.10.0

# ── Computer Vision ────────────────────────────────────────
# MediaPipe requires contrib. Use only one distribution providing the cv2 namespace.
# Docker already installs the native libraries required by this build.
opencv-contrib-python==4.11.0.86
mediapipe==0.10.9
Pillow==10.2.0

# ── Text-to-Speech ─────────────────────────────────────────
gtts==2.5.1
# pyttsx3 needs native audio drivers and is not used server-side; keep optional.
# pyttsx3==2.90
